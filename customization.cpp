#ifndef headers // #include guards
#define headers

#include <iostream>
#include <string>
#include <vector>
#include <algorithm>

#endif // headers

/**
 * custom variables
 */
std::string escapeSequence = "?";

char op_not = '-'; // not
char op_and = '&'; // and
char op_or = '|';  // or
char op_xor = '^'; // xor
